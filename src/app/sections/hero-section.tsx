/** Hero section — the page's lead block. */
export default function HeroSection() {
  return (
    <div className="h-full block fixed top-0 inset-x-0 z-9999 pointer-events-none" id="hs-web-interactives-top-anchor" />
  );
}
